import ProfileButton from "./ProfileButton";
import ProfileData from "./ProfileData";

const AppProfile = () => {
  return (
    <aside className="max-w-[19.75rem] m-auto h-full">
      <ProfileData />
      <ProfileButton />
    </aside>
  );
};

export default AppProfile;
