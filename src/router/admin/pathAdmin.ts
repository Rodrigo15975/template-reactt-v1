export enum PathsAdminProtected {
  // EXAMPLE
  DASHBOARD = "/dashboard",
  PRODUCTS = "/products",
  LIST_ORDER = "/list-order",
  ADMINISTRATOR = "/admin-administrator",
  REPORTS = "/reports",
  HISTORY_ORDER = "/history-order",
  CATEGORIES = "/categories",
}
export enum PathsAdminPublic {
  // EXAMPLE
  LOGINADMIN = "/",
}
